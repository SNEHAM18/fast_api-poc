  <!-- uvicorn main:app --reload
  
        uv run  main.app -->
to run docker image --> docker build -t fast-api .
-->for container------> docker run -p 8000:8000 fast-api

      <!-- uv add "fastapi[standard]" --->